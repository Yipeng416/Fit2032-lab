specVersion: "v1beta"
serviceId: "NoMash-Library"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "NoMash-Library-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
